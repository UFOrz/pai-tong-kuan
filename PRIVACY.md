# 拍同款隐私政策

更新日期：2026 年 7 月 22 日

“拍同款”是一款由用户自行配置 AI 平台 API Key 的 Chrome 扩展，用于分析用户主动选择的网页图片、生成复刻提示词及同款图片，并在本机管理生成记录。

## 处理的数据

扩展仅为实现用户主动使用的功能而处理以下数据：

- API 配置：平台名称、Base URL、模型名称及用户填写的 API Key。
- 用户主动选择的内容：网页图片、用户主动框选截取的当前可见页面区域、图片地址、所在页面 URL 和页面标题。
- 替换参考素材：用户添加到本机角色与物品库中的图片和名称。
- 用户生成内容：反推提示词、用户编辑后的提示词、生成图片及生成参数。
- 本地任务状态：当前任务、模型选择、相册记录和界面设置。

扩展不收集姓名、邮箱、通讯录、精确位置、支付卡信息，也不创建跨网站用户画像。

## 数据用途与共享

- 只有当用户点击反推、生成、替换角色或组图生成时，扩展才会将完成该任务所必需的来源图、参考素材和提示词发送到用户在设置中选定的 AI 平台。
- API Key 仅用于向该平台鉴权。扩展开发者不运营中转服务器，也不会接收用户的 API Key、图片或提示词。
- 内置支持的平台包括 OpenAI、ModelScope、SiliconFlow、Agnes-AI、ZenMux、RunningHUB、AtlasCloud 和 APImart。用户也可以配置自己的兼容服务地址。
- 部分 Get Key 链接属于平台邀请或推广链接。只有在用户主动点击后才会打开；开发者可能获得平台提供的推广奖励，但不会因此提高用户的使用价格。扩展不会自动跳转、替换网页链接、写入推广 Cookie，也不会跟踪用户是否注册或购买。
- 第三方平台如何保存和处理请求，由相应平台的隐私政策和用户与该平台之间的协议决定。用户应在使用前阅读所选平台的政策。

除完成用户明确请求的反推、生图，或法律、安全所必需的情形外，扩展不会向其他第三方传输数据。

## 本地保存与删除

- API 配置保存在 `chrome.storage.local`，并限制为扩展可信页面和后台访问。
- 当前任务临时保存在 `chrome.storage.session`，浏览器会话结束或扩展被重新加载后可能被清除。
- 来源图、角色/物品参考素材、提示词和生成图片保存在本机 IndexedDB 中。
- 用户可以在设置页删除 API Key，在角色与物品库删除参考素材，在相册中删除单条或多条作品；卸载扩展会清除 Chrome 管理的扩展本地数据。

## 数据安全

- 预设 AI 平台均使用 HTTPS。
- 自定义 API Base URL 必须使用 HTTPS；仅本机 `localhost` 调试允许 HTTP。
- 扩展不加载或执行远程 JavaScript，全部运行逻辑均包含在扩展安装包中。

## 广告、出售与人工读取

扩展不会出售用户数据，不会将用户数据用于个性化广告、再营销、信用评估或数据经纪，也不会允许开发者或其他人员读取用户内容；法律义务或用户明确请求的支持情形除外。

扩展对用户数据的使用遵守 Chrome Web Store User Data Policy，包括 Limited Use 要求。

## 儿童隐私

扩展并非专门面向儿童，也不会有意收集儿童个人信息。

## 政策变更与联系

如数据处理方式发生实质变化，扩展会在实施前通过产品界面进行提示，并在需要时重新征得同意。有关隐私的问题，可通过 Chrome Web Store 商品页面的“支持”入口联系发布者。
